# Node
node_modules/
.next/
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Data
/data/
*.parquet

# Raw .sav source files dropped at repo root before ingestion (real respondent
# data, never committed/pushed) — landing files under data/landing are already
# covered by /data/ above; this covers ones staged at root before that move.
/*.sav

# Env
.env
.env.*
apps/web/.env.local
apps/web/.env.*

# Supabase local contract draft (do not commit)
supabase/sql/001_bbs_rpc_contract.sql

# OS
.DS_Store
Thumbs.db

# Agent control files (local-only, not shared)
BITACORA.md
CLAUDE.md
